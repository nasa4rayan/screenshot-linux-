# screenshot-linux-

SnapKey is a lightweight Linux screenshot/recording helper focused on keyboard shortcuts.

## Repository layout

- `snapkey/` → project source, installer, systemd user unit, and detailed docs.

## Quick start

```bash
cd snapkey
chmod +x install.sh
./install.sh
```

For full documentation and troubleshooting, see `snapkey/README.md`.
